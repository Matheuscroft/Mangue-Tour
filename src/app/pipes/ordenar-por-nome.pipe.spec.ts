import { OrdenarPorNomePipe } from './ordenar-por-nome.pipe';

describe('OrdenarPorNomePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorNomePipe();
    expect(pipe).toBeTruthy();
  });
});
