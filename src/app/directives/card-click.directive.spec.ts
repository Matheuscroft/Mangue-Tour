import { CardClickDirective } from './card-click.directive';

describe('CardClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CardClickDirective();
    expect(directive).toBeTruthy();
  });
});
